import styled from 'styled-components/macro';

const Container = styled.div`
    
`

const Wrapper = styled.div`
    
`

const Home = () => {
    return (
        <Container>
            <Wrapper>
                Events
            </Wrapper>
        </Container>
    );
}

export default Home;